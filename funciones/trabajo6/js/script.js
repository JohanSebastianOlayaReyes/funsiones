function promedio(pnumUno, pnumDos, pnumTres){
    let numUno = pnumUno;
    let numDos = pnumDos;
    let numTres = pnumTres;
    let promedio;
    promedio = (numUno + numDos + numTres) / 3;
    return  promedio;
}