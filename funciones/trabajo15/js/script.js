function contador(Pnumero){
    let contador;
    let numero;
    contador = 0;
    numero = Pnumero;
    while(contador < numero){
        contador = contador + 1;
        console.log(contador);
    }
}